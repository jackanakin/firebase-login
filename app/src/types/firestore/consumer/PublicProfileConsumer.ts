export const PublicProfileConsumerCollection = "PublicProfileConsumer";

/**
 * PublicProfileConsumer/{consumer_uid}
 */
export interface PublicProfileConsumer {
    owner_uid: string;
    name: string;
    city: string;
}